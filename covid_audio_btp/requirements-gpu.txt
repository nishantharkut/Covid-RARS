# Install PyTorch from the official selector for your CUDA/driver setup when enabling CNN.
# Example for recent CUDA-compatible wheels, if supported by your system:
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128
transformers>=4.40
accelerate>=0.30
safetensors>=0.4
