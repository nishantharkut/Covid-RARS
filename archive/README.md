# Archive

This directory contains historical material that is useful for traceability but is not the active code path.

| Path | Contents |
|---|---|
| `patches/` | Historical patch files |
| `updates/` | Older update/run notes |
| `review_materials/` | Gemini/PDF review exports and duplicated review snapshots |

Archive files should not be used as the primary evidence source unless the active docs explicitly point to them.
